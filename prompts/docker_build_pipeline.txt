A clean educational flowchart showing the Docker build pipeline from left to right. Three stages connected by large arrows: STAGE 1 "Dockerfile" - a text file icon with visible lines of code (FROM, WORKDIR, COPY, RUN, CMD). Arrow labeled "docker build" leads to STAGE 2 "Image" - a blue layered box stored on disk, with a small Docker Hub cloud icon above it labeled "or pull from Docker Hub". Arrow labeled "docker run" leads to STAGE 3 "Container" - a green running box with a play button. Below the container, two small icons: a folder icon labeled "Volumes (-v)" and a plug icon labeled "Ports (-p)". Clean flat design, white background, educational diagram style, minimal text.