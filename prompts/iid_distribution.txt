Educational diagram showing a probability distribution D over (x, y) pairs for machine learning. Show a 2D bell curve / joint distribution surface with axes labeled "x (input features)" and "y (target label)". Show arrows pointing from the distribution to "Training samples" (a small cluster of dots drawn from the distribution) and "Test samples" (another small cluster of dots drawn from the same distribution). Include the text "Both drawn i.i.d. from the same distribution D". Clean, minimal academic style with white background, suitable for lecture slides. No decorative elements.