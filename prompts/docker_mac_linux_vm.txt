A clear educational diagram explaining how Docker works on a Mac. Three nested layers shown as boxes: OUTERMOST layer labeled "Your Mac (macOS)" in gray. MIDDLE layer labeled "Docker Desktop's Hidden Linux VM" in light blue, with a small note "tiny, invisible, runs automatically". INNERMOST layer shows three small colored containers side by side labeled "Debian", "Ubuntu", "Alpine" - all sharing the Linux VM's kernel. A key insight callout: "Containers share the Linux VM's kernel, not macOS". The diagram should make it crystal clear that on Mac, Docker runs a Linux VM first, then containers run inside that VM. Clean flat design, white background, educational style.