An educational diagram explaining the KS test (Kolmogorov-Smirnov test) intuitively. Shows two cumulative distribution function (CDF) curves on the same axes - one blue labeled "Training data" and one red labeled "Production data". A vertical double-headed arrow between the two curves at their maximum distance point, labeled "D = maximum gap". Below the chart, a simple rule: "If D is large → distributions are different → DRIFT DETECTED". Clean, minimal educational style, white background, large readable labels.