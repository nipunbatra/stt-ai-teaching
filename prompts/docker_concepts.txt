Clean educational infographic diagram showing Docker concepts. Three main sections: Left shows a "Dockerfile" (recipe card icon with code lines), center shows an arrow labeled "docker build" pointing to a "Docker Image" (blue box/blueprint icon), then another arrow labeled "docker run" pointing to a "Container" (green running instance icon with a play button). Below, show the key insight: "Image = Recipe, Container = Cooked Dish". At the bottom, show three identical containers running on different machines (laptop, cloud server, colleague's computer) all from the same image, illustrating portability. Title: "Docker: Build Once, Run Anywhere". Clean white background, modern flat design, blue/green color scheme, bold readable text. Educational slide infographic style.