Clean educational diagram showing polynomial regression fitting the same data at three different complexities. Three side-by-side panels on white background. Each panel has the same scattered blue data points (about 20 points following a slight curve). Panel 1: "Degree 1 (Underfitting)" — a straight red line that misses the curve, labeled "Too simple". Panel 2: "Degree 3 (Just Right)" — a smooth green curve that follows the data trend nicely, labeled "Good fit". Panel 3: "Degree 15 (Overfitting)" — a wildly oscillating orange curve that passes through every point but wiggles crazily between them, labeled "Too complex". Below all three, an arrow from "High Bias, Low Variance" to "Low Bias, High Variance". Title: "Model Complexity: Polynomial Degree". Clear axis labels (x, y). Modern flat educational style, bold readable text.