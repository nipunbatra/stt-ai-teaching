A clean educational diagram showing Docker Image vs Container concept. LEFT SIDE labeled "Image (Blueprint)": A blue rectangular box with rounded corners containing stacked layers labeled "Python 3.10", "scikit-learn", "app.py" with a lock icon and text "Read-only, frozen on disk". RIGHT SIDE labeled "Container (Running Instance)": Three separate green boxes, each a copy spawned from the image with an arrow from the image to each, showing they are alive running processes. One container has a small file being written to show it can read/write. Arrow between image and containers labeled "docker run". Clean flat design, white background, educational style, warm color palette with blue for images and green for containers.